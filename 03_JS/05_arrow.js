const add = (a, b) => {
  return a + b;
};

const add2 = (a, b) => a + b;

const toUpperCase = (str) => str.toUpperCase();

const sumArray = (arr) => {
  let sum = 0;
  for (let i = 0; i < arr.length; i++) {
    sum += arr[i];
  }
  return sum;
};
